# docker/docker-compose.yml

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - LOG_LEVEL=20
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - HUGGING_FACE_USER_ACCESS_TOKEN=${HUGGING_FACE_USER_ACCESS_TOKEN}
    volumes:
      - ./data:/app/data
      - ./app/_configs.yaml:/app/_configs.yaml
      - ./frontend:/frontend
    restart: unless-stopped
